# compsval engine

The project README lives at the repository root: [../README.md](../README.md).

This directory contains the Python package `compsval`, its test suite, the
upstream provenance audit ([UPSTREAM.md](UPSTREAM.md)) and the archived
upstream license ([upstream/](upstream/)).
